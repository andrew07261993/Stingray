# STINGRAY Retrieval Gaps

The ChatGPT-side harvest intentionally avoided the broken account-export path. The following still require local Codex inspection on ANDREWSPC.

## Required local recovery

1. Resolve the exact repository/worktree, branch, commit and source files for `STINGRAY_I5S_DF8_SHORT14_FORWARD`.
2. Inventory all STINGRAY Git repositories/worktrees under the user's Codex/Documents working areas, including local-only branches not pushed to GitHub.
3. Recover relevant Codex Desktop/CLI session history from locally accessible session/archive stores.
4. Where Codex Desktop exposes synchronized Remote session history, ingest the STINGRAY-relevant sessions that are actually accessible; do not invent inaccessible cloud-only content.
5. Index local STEP/AP242, Creo, ZIP, PDF, XLSX/CSV, scripts, reports, screenshots, vendor CAD/data and validation outputs not already represented in GitHub.
6. Reconcile the staged-inflation branch against the later SHORT14/FORWARD external-buoy architecture.
7. Resolve the latest validation state of the shortened/external-buoy model; do not assume R2 parity automatically applies.
8. Recover exact outputs from the targeted COTS Phase 1/Phase 2 work and any COTS-heavy alternative branches that exist only locally.
9. Recover exact local vendor-CAD/procurement evidence and RFQ responses where they materially affect part selection.

## ChatGPT limitation

This context harvest used the accessible project/history context and File Library evidence available in ChatGPT. It is not a byte-for-byte export of every historical conversation. Important engineering conclusions were consolidated with status/provenance labels so the project can proceed without requiring an account data export.

## Security exclusion

Do not ingest passwords, passkeys, API keys, tokens, cookies, credential databases or unrelated personal data while closing these gaps.