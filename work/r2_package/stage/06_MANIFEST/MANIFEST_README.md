# Controlled R2 Candidate WIP Manifest

Exact package status: `CREO_VALIDATION_PENDING — WIP — NOT RELEASED`

`file_manifest.csv` hashes every payload/control file that existed before the manifest itself was written. `SHA256SUMS.txt` then hashes every archive file except itself, avoiding an impossible self-referential digest.
